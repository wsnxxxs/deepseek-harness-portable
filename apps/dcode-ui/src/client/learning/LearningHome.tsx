/**
 * Compatibility entry kept for consumers that imported the old path.
 * The workbench now presents the resource library as the top-level surface;
 * learning is a section inside it.
 */
export { ResourceLibraryHome as LearningHome } from '../library/ResourceLibraryHome.tsx'
export type { ResourceLibraryHomeProps as LearningHomeProps } from '../library/ResourceLibraryHome.tsx'
