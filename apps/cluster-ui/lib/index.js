import { CLUSTER_SERVICE } from "./contract.js";
//#region lib/types/index.js
/** Stable Cordis plugin name. */
const name = "cluster-ui";
/** Host plugin body; every Cluster behavior lives in the browser half. */
function apply() {}
//#endregion
export { CLUSTER_SERVICE, apply, name };
