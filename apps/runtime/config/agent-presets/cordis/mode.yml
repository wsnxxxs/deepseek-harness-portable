id: cordis
baseConfig: agent.cordis.yml

contract:
  persona:
    complete: true
    includeRuntimeContext: true
  tools:
    requiredRows:
      - tool-fs
      - tool-fs-search
      - tool-jobs
      - tool-skill
      - tool-goal
      - tool-subagent-control
      - tool-subagent-list-agents
      - tool-subagent
      - tool-subagent-fork
      - tool-workflow
      - tool-ralph
      - tool-ask-user
      - tool-todo
      - tool-web
      - tool-cordis
    variantSlots:
      shell:
        - tool-bash
        - tool-pwsh
  composition:
    requiredRows:
      - persona
      - agent-instructions
      - plan-mode
      - compaction-basic
      - command-compact
      - tool-result-pruner
  compaction: enabled
  filesystem: policy-mediated
  sandbox: workspace-write
  selfModification: cordis-plugin-runtime

variants:
  - id: win32-powershell
    supportLevel: native
    requires:
      - shell.powershell
      - sandbox.workspace-write
    acceptsDegraded:
      - sandbox.workspace-write
    config: variants/win32-powershell.cordis.yml
    provides:
      shell: tool-pwsh
