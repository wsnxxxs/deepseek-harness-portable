DeepSeek Harness for Win v1.6.0
================================

Chinese guide: 使用说明.txt

1. DeepSeek Harness Launcher.exe
   Launches the bundled Electron desktop shell without opening a command window.
   start-desktop.cmd remains available for console diagnostics and update recovery.

2. start-web.cmd
   Uses Electron's embedded Node.js to launch the Web surface; no system Node.js is required.

3. dsh.cmd or update.ps1
   Checks the latest distribution, downloads a complete release, verifies SHA-256,
   validates the release manifest, and replaces runtime as one operation. The desktop
   shell keeps running until the package is ready and restart is confirmed.
   Run dsh clean or update.cmd clean to purge committed update backups and temporary update cache.

4. Release Notes
   Open the tray menu to read the current release, release history, and component
   versions. The desktop shell uses the GitHub mirror and a bundled local fallback.
   Update notices are centered below the title bar, slide away and are destroyed
   after seven seconds or dismissal, and can be suppressed per version.
   If an online check fails, the Update Hub shows an error state with a retry
   action instead of blocking the main window.

5. Native Logo menu
   When the sidebar is expanded, click the blue native logo to open the desktop
   menu. When collapsed, left click still expands the sidebar and right click
   opens the desktop menu.

6. setup-shortcuts.ps1
   Creates a desktop shortcut and adds this portable root to the user PATH.

7. uninstall.cmd or uninstall.ps1
   Removes the portable application, shortcuts, and PATH entry. User data is kept
   unless you explicitly confirm its removal.

The marketplace is preinstalled once per Web profile and can be disabled or
removed under Settings → Plugins → Installed; restart does not restore it.
Marketplace plugins are third-party code, so review their source and permissions.

This release is not signed by a trusted commercial CA. SmartScreen or Smart App
Control may warn or block it. No certificate or trust-store change is performed
automatically. Verify SHA256SUMS.txt before running downloaded files.

All application files are under runtime/. Do not delete or rename that directory.
